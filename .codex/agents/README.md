# Codex custom agents

Tạo `<ten-agent>.toml` tại đây khi cần agent chuyên trách. Cấu hình tối thiểu gồm `name`, `description` và `developer_instructions`. Chưa có agent tùy chỉnh nào được định nghĩa.
